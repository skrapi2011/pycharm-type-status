Variable Type in Statusbar

This PyCharm plugin is work-in-progress and will allow displaying variable type in the status bar.
For variable type to show up, we need to place a caret on given variable.
